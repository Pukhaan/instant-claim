"use client";

// PhotoUpload — drag/drop or tap-to-capture photo input.
// On mobile, use <input type="file" accept="image/*" capture="environment" />
// to open the camera directly. Show a thumbnail preview once picked.

export function PhotoUpload(_props: {
  value: File | null;
  onChange: (f: File | null) => void;
}) {
  // TODO(frontend): implement.
  return null;
}
